#include <bits/stdc++.h>

#define rep(i,a,n)      for(int i = a ; i < n ; i++)
#define ln cout<<"\n";
#define debug(x) cout<<(x)<<"\n"
#define ll long long

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	
	int lines = 0;
	cin >> lines;
	
	rep(i, 0, lines){
		int n;
		cin >> n;
		
		debug(n);
	}
	

	return 0;
}
